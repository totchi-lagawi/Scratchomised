package io.github.totchi_lagawi.scratchomised_plugin.utils.http;

/**
 * Enumeration of the different HTTP methods
 * 
 * @implNote Only GET was required, hence it is the only one defined
 */
public enum HTTPMethod {
    GET,
    UNKNOWN
}